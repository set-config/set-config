{
  "name": "set-config",
  "version": "1.0.0",
  "private": true,
  "packages": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter=@set-config/core build && pnpm -r --filter=@set-config/dotenv build && pnpm -r --filter=@set-config/yaml build && pnpm -r --filter=@set-config/toml build && pnpm -r --filter=@set-config/markdown build && pnpm -r --filter=@set-config/cli build",
    "test": "vitest run",
    "test:integration": "CI=true vitest run integration/"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "npm:rolldown-vite@latest",
    "vitest": "^4.1.1"
  },
  "pnpm": {
    "overrides": {
      "vite": "npm:rolldown-vite@latest"
    }
  }
}
