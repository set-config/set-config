{
  "name": "@set-config/dotenv",
  "version": "0.2.1",
  "description": "ENV adapter for set-config - .env file support",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "set-config": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "node scripts/build.ts",
    "prepublishOnly": "exit 1",
    "publish": "cd dist && npm publish"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "npm:rolldown-vite@latest"
  },
  "dependencies": {
    "@set-config/core": "workspace:*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/set-config/set-config"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
